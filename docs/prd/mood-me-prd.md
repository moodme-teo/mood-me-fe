# PRD: 추구미 무드보드 서비스

## 1. 개요

### 1.1 배경

사용자가 자신이 "추구하는 분위기(추구미)"를 이미지·키워드 선택 기반의 간단한 테스트로 표현하면, AI가 이를 시각적 무드보드로 자동 생성해주고 사용자가 직접 편집·완성하여 SNS에 공유할 수 있는 서비스.

### 1.2 용어 정의

| 용어          | 정의                                                             |
| ------------- | ---------------------------------------------------------------- |
| 추구미        | 사용자가 지향하는 스타일/분위기 (미적 취향)                      |
| 추구미 테스트 | 이미지/키워드 선택을 통해 사용자의 취향을 수집하는 질문 플로우   |
| 무드보드      | 선택 결과를 바탕으로 AI가 생성한, 이미지·색·키워드로 구성된 보드 |
| 비회원        | 카카오 로그인을 하지 않고 서비스를 이용하는 사용자               |

## 2. 목표

- 사용자가 로그인 장벽 없이 즉시 무드보드 생성 경험을 체험할 수 있게 한다.
- AI 생성 대기 시간을 지루하지 않게 만들어 이탈을 최소화한다.
- 결과물(무드보드 + 성향 그래프)의 SNS 공유를 유도해 자연 유입을 만든다.

### 성공 지표 (제안, 추후 확정 필요)

- 추구미 테스트 완료율 (테스트 시작 → 생성 요청)
- 무드보드 저장/내보내기 전환율
- SNS 공유율
- 비회원 → 회원 전환율 (저장/공유 시점 로그인 유도 기준)

## 3. 대상 사용자 및 접근 권한

| 기능                           | 비회원                      | 회원(카카오 SSO) |
| ------------------------------ | --------------------------- | ---------------- |
| 메인 페이지 열람               | ✅                          | ✅               |
| 추구미 테스트 진행             | ✅                          | ✅               |
| 무드보드 생성                  | ✅                          | ✅               |
| 무드보드 편집                  | ✅                          | ✅               |
| 결과 열람 (당일 세션 내)       | ✅                          | ✅               |
| 무드보드 영구 저장/재방문 조회 | ❌ (세션 종료 시 소실 가능) | ✅               |
| SNS 공유 / 이미지 내보내기     | ✅                          | ✅               |

> 비회원도 전체 플로우(테스트→생성→편집→결과·공유·내보내기)를 끝까지 이용 가능하게 하되, 결과물을 나중에 다시 보려면 로그인이 필요하다는 점을 결과 페이지에서 안내한다.

## 4. 전체 유저 플로우

```
[로그인/비회원 진입]
        │
        ▼
   [메인 페이지] ──(무드보드 만들기 클릭)──▶ [추구미 테스트]
                                                  │ (질문 N개 반복)
                                                  ▼
                                          [무드보드 생성중]
                                                  │ (AI 생성 완료)
                                                  ▼
                                            [무드보드 편집]
                                                  │ (저장)
                                                  ▼
                                          [최종 결과물 페이지]
                                          ├─ SNS 공유
                                          └─ 이미지 내보내기
```

## 5. 화면별 상세 명세

### 5.1 로그인

- **경로:** `/login` (또는 메인 진입 시 모달)
- **구성:** 카카오 SSO 로그인 버튼, "비회원으로 계속하기" 버튼
- **동작:**
  - 카카오 로그인 성공 시 → 메인 페이지로 이동, 세션에 사용자 식별자 저장
  - 비회원 선택 시 → 클라이언트에 게스트 세션 ID 발급 후 메인 페이지로 이동
- **예외:** 카카오 인증 실패/취소 시 로그인 화면에 에러 토스트 노출, 재시도 가능

### 5.2 메인 페이지

- **경로:** `/`
- **구성:**
  - 서비스(무드보드) 설명 영역
  - 무드보드 예시 이미지 (캐러셀 또는 그리드, 다른 사용자 결과물 또는 큐레이션 샘플)
  - "무드보드 만들기" CTA 버튼
- **동작:** CTA 클릭 시 추구미 테스트 첫 질문으로 이동, 새 세션(테스트 세션) 생성
- **비회원 처리:** 별도 제약 없음

### 5.3 추구미 테스트

> 상세 스펙(카드 36장·그림자 8개·전환 32개·카피·로깅 스키마·Claude 해석 프레임): [docs/work/todo/mood-test-questions.md](../work/todo/mood-test-questions.md) — **7/7 확정, 이 문서가 구현 기준**

- **경로:** `/test/:sessionId`
- **구조:** 단발 객관식이 아닌 **3막 선택 메커니즘** (목표 2~3분) — 행동 데이터(무엇을 언제 버렸는지)가 곧 분석 재료
  1. **담기** — 카드 그리드 36장 중 끌리는 12장 담기
  2. **덜어내기 ×2** — 12→8→5장 강제 덜어내기. 남은 5장 = **확신의 가치**
  3. **그림자** — "요즘 나를 무겁게 하는 것" 칩 8개 중 3개
  4. **전환** — 그림자마다 4지선다(뻔한 반대말 1 + 결이 다른 해석 3). 선택 = **열망의 가치**
  5. **최종 대결** — 확신 5장 + 열망 3장 중 무드보드에 남길 5장 선택
- **상단 헤더:**
  - 뒤로가기 버튼 (이전 단계로, 첫 단계에서는 메인으로)
  - 진척도 표시 (단계 기준, 프로그레스바)
  - 프리뷰: 덜어내기에서 **남은 카드가 보드에 자리를 잡아가는** 연출 (상실이 아니라 확정의 경험)
- **여정 로깅:** `{selected, dropped_r1/r2, survivors, shadows, transitions, final, dropped_final, toggles}` — 전 과정이 세션에 저장되어 Claude 분석 입력이 된다
- **상태 저장:** 뒤로가기로 이전 단계 이동 시 이전 선택값 유지 (재선택 가능)

### 5.4 무드보드 생성중

- **경로:** `/test/:sessionId/generating`
- **구성:**
  - 사용자가 선택한 이미지들이 가상의 무드보드 레이아웃 위에 순차적으로 배치되며 채워지는 인터랙션 애니메이션
  - 프로그레스바
  - 프로그레스바 상단 상태 메시지: "AI가 그리는 중..." 류의 위트있는 문구, 일정 간격(예: 2~3초)마다 로테이션
- **동작:**
  - 백그라운드에서 실제 AI 생성 작업 진행 상태를 폴링/구독하고, 진행률에 맞춰 프로그레스바 갱신
  - 생성 완료 시 자동으로 무드보드 편집 화면으로 이동
- **예외:** 생성 실패 시 에러 화면 + "다시 시도" 버튼 (추구미 테스트 결과는 유지한 채 재생성 요청)

### 5.5 무드보드 편집

- **경로:** `/moodboard/:moodboardId/edit`
- **구성:**
  - AI가 완성한 무드보드 캔버스
  - 편집 도구: 펜(자유 드로잉), 스티커(스티커 팔레트에서 추가), 글자(텍스트 박스 추가/폰트·색상), 드래그(요소 위치 이동/리사이즈)
  - 저장 버튼
- **동작:**
  - 각 도구는 캔버스 위 요소로 추가되며 개별 이동/삭제 가능
  - 저장 시 현재 캔버스 상태(요소 좌표·속성 포함)를 서버에 저장 후 결과물 페이지로 이동
- **비회원 처리:** 저장은 게스트 세션 기준으로 임시 저장, 로그인 시 계정에 귀속

### 5.6 최종 결과물 페이지

- **경로:** `/moodboard/:moodboardId`
- **구성:**
  - 상단: 완성된 무드보드 이미지
  - 하단: 나의 무드 성향 그래프 (추구미 테스트 답변 기반 축 시각화, 예: 레이더 차트)
  - SNS 공유 버튼 (카카오톡 공유, 링크 복사, 이미지 공유 등)
  - 이미지 내보내기 버튼 (PNG/JPG 다운로드)
- **동작:**
  - 공유 시 공유용 썸네일/OG 이미지 생성
  - 내보내기 시 캔버스를 이미지 파일로 렌더링하여 다운로드
- **비회원 처리:** 페이지 진입 시 "로그인하면 언제든 다시 볼 수 있어요" 안내 배너 노출 (저장 유도)

## 6. 기능 요구사항 목록

| ID   | 기능명                           | 페이지        |
| ---- | -------------------------------- | ------------- |
| F-01 | 카카오 SSO 로그인                | 로그인        |
| F-02 | 비회원 게스트 세션 진입          | 로그인        |
| F-03 | 무드보드 서비스 설명 노출        | 메인          |
| F-04 | 무드보드 예시 이미지 노출        | 메인          |
| F-05 | 무드보드 만들기 진입             | 메인          |
| F-06 | 테스트 뒤로가기/진척도 표시      | 추구미 테스트 |
| F-07 | 선택 기반 실시간 프리뷰 이미지   | 추구미 테스트 |
| F-08 | 카드 담기 (36장 중 12장)         | 추구미 테스트 |
| F-09 | 강제 덜어내기 ×2 (12→8→5)        | 추구미 테스트 |
| F-10 | 그림자 선택 + 전환 4지선다       | 추구미 테스트 |
| F-11 | 최종 대결 (확신+열망 8장 중 5장) | 추구미 테스트 |
| F-12 | 무드보드 AI 생성 요청/진행       | 생성중        |
| F-13 | 생성 과정 인터랙션 애니메이션    | 생성중        |
| F-14 | 로테이션 상태 메시지             | 생성중        |
| F-15 | 생성 진행률 프로그레스바         | 생성중        |
| F-16 | 펜(드로잉) 편집 도구             | 편집          |
| F-17 | 스티커 편집 도구                 | 편집          |
| F-18 | 글자(텍스트) 편집 도구           | 편집          |
| F-19 | 요소 드래그 이동/리사이즈        | 편집          |
| F-20 | 무드보드 저장                    | 편집          |
| F-21 | 완성 무드보드 열람               | 결과물        |
| F-22 | 무드 성향 그래프 시각화          | 결과물        |
| F-23 | SNS 공유                         | 결과물        |
| F-24 | 이미지 내보내기                  | 결과물        |

## 7. 데이터 모델 (초안)

```
User
  - id
  - kakao_id
  - nickname
  - profile_image_url
  - created_at

GuestSession
  - id (client-issued)
  - created_at
  - expires_at

MoodTestSession
  - id
  - user_id (nullable)
  - guest_session_id (nullable)
  - status: in_progress | completed
  - created_at

MoodTestAnswer
  - id
  - test_session_id
  - question_id
  - answer_type: image | keyword | image_keyword
  - selected_image_ids: []
  - selected_keywords: []
  - custom_text
  - order

MoodboardGenerationJob
  - id
  - test_session_id
  - status: queued | processing | completed | failed
  - progress_percent
  - status_message
  - result_moodboard_id (nullable)

Moodboard
  - id
  - user_id (nullable)
  - guest_session_id (nullable)
  - base_image_url (AI 생성 결과)
  - elements: [MoodboardElement]
  - mood_profile: {axis: value, ...}
  - created_at
  - updated_at

MoodboardElement
  - id
  - moodboard_id
  - type: pen | sticker | text
  - properties (좌표, 크기, 색상, 폰트, 스티커 종류 등)
  - z_index
```

## 8. API 명세 (초안)

| Method | Endpoint                            | 설명                                 |
| ------ | ----------------------------------- | ------------------------------------ |
| POST   | `/auth/kakao/callback`              | 카카오 OAuth 콜백 처리, 세션 발급    |
| POST   | `/guest-sessions`                   | 게스트 세션 발급                     |
| GET    | `/mood-test/questions`              | 테스트 시드 조회 (카드 36·그림자 8·전환 32) |
| POST   | `/mood-test-sessions`               | 테스트 세션 시작                     |
| POST   | `/mood-test-sessions/{id}/answers`  | 단계별 여정 로그 저장 (선택·탈락·토글)  |
| POST   | `/mood-test-sessions/{id}/generate` | 무드보드 생성 요청 (비동기 job 생성) |
| GET    | `/generation-jobs/{id}`             | 생성 진행 상태 폴링                  |
| GET    | `/moodboards/{id}`                  | 무드보드 상세 조회                   |
| PATCH  | `/moodboards/{id}`                  | 편집 내용(elements) 저장             |
| POST   | `/moodboards/{id}/export`           | 이미지 파일 렌더링/다운로드 URL 발급 |
| POST   | `/moodboards/{id}/share`            | 공유용 썸네일/링크 생성              |

## 9. AI 생성 파이프라인 개요

> 상세: [mood-test-questions.md](../work/todo/mood-test-questions.md)의 Claude 해석 프레임·부록(프롬프트 전문)

1. 테스트 세션의 **여정 로그 전체**(선택·라운드별 탈락·그림자·전환·최종 대결·토글)를 취합
2. 서버가 **Claude 페이로드로 변환** — id → 라벨·태그 조인, `not_picked`·`was_obvious_antonym` 포함
3. **Claude 한 호출**로 무드 프로파일 산출: 타이틀 / 확신·열망 해석 / 무드 벡터 5축 / 키워드 9 / 스티커 문구 3 / 유형명(미학 코어 × 인생 테마) / 이미지 생성 프롬프트
4. **보드 재료 매핑**: 확신 카드 → 큐레이션 타일(카드 이미지 그대로), 열망 → fal.ai 생성 컷(`image_prompt`)
5. 진행률을 클라이언트에 전달 (폴링), 조립 완료 시 결과 이미지를 `Moodboard.base_image_url`로 저장, 편집 화면으로 전환

### 9.1 구현 단계 (빌더 — 담당: hyeon-aa)

| 순서 | 이슈 | 내용 | 의존 |
| --- | --- | --- | --- |
| ③ | [#34](https://github.com/moodme-teo/mood-me-fe/issues/34) | 시드 데이터(카드 36·그림자 8·전환 32) + 세션 API + 불변식 검증 | — |
| ④ | [#35](https://github.com/moodme-teo/mood-me-fe/issues/35) | 테스트 플로우 UI (3막 + 여정 로깅 + 프리뷰 연출) | ③과 일부 병행 |
| ⑤ | [#36](https://github.com/moodme-teo/mood-me-fe/issues/36) | Claude 분석 Route Handler (페이로드 변환 → 프롬프트 → 출력 JSON) | ③ |
| ⑥ | [#37](https://github.com/moodme-teo/mood-me-fe/issues/37) | 생성 파이프라인 (큐레이션 타일 + fal.ai 컷 → 보드 조립 → 폴링) | ⑤ |

## 10. 엣지 케이스 및 에러 처리

| 상황                            | 처리 방안                                                      |
| ------------------------------- | -------------------------------------------------------------- |
| 카카오 로그인 실패/취소         | 에러 토스트, 로그인 화면 유지, 비회원 진입 유도                |
| 테스트 중 이탈 후 재방문        | 회원: 이어하기 가능 / 비회원: 세션 만료 전까지만 유지          |
| 생성 실패                       | 재시도 버튼 제공, 기존 답변 유지                               |
| 생성 시간 과도 지연             | 일정 시간 초과 시 "조금만 더 기다려주세요" 등 안내 메시지 전환 |
| 편집 중 저장 실패               | 로컬 임시 저장(autosave) 후 재시도, 사용자에게 실패 알림       |
| 비회원 세션 만료 후 결과물 접근 | 결과물 소실 안내 + 로그인 시 저장 가능하다는 안내              |
| 이미지 내보내기 실패            | 재시도 옵션 제공                                               |

## 11. 비기능 요구사항

- **성능:** 생성 대기 화면은 실제 처리 시간과 무관하게 지루하지 않게 인터랙션·메시지로 체감 대기시간을 줄인다.
- **반응형:** 모바일 우선 (SNS 공유 대상이 모바일일 가능성이 높음).
- **접근성:** 편집 도구(펜/스티커/글자)는 터치 조작 기준으로 설계.
- **개인정보:** 카카오 SSO 시 수집 동의 항목(닉네임, 프로필 이미지 등) 명시.
- **콘텐츠 라이선스:** 예시 이미지·스티커 에셋의 저작권/라이선스 확인 필요.

## 12. Out of Scope (이번 범위 아님)

- 다른 사용자의 무드보드 피드/탐색 기능
- 무드보드 댓글/좋아요 등 소셜 인터랙션
- 사용자 간 팔로우/프로필 기능
- 무드보드 버전 히스토리 관리

## 13. Open Questions (확정 필요)

1. 비회원 결과물의 보관 기간(세션 만료 정책)은 얼마로 설정할까?
2. ~~추구미 테스트 질문 개수·구성은 몇 개로 확정할까?~~ → **해소 (7/7)**: 3막 메커니즘으로 확정 — [mood-test-questions.md](../work/todo/mood-test-questions.md)
3. ~~무드 성향 그래프의 축은 어떤 기준으로 정의할까?~~ → **해소 (7/7)**: 5축 확정 (고요↔활기 · 따뜻↔서늘 · 미니멀↔맥시멀 · 빈티지↔모던 · 현실↔몽환)
4. 이미지 생성 모델/외부 API는 무엇을 사용할까? (비용·응답 속도 영향)
5. SNS 공유는 어떤 채널까지 지원할까? (카카오톡/인스타그램 스토리/링크 복사 등)
